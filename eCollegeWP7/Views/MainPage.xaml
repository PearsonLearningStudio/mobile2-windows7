<phone:PhoneApplicationPage 
    x:Class="eCollegeWP7.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800" 
    d:DataContext="{d:DesignData SampleData/MainViewModelSampleData.xaml}"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
 
        <!--Panorama control-->
        <controls:Panorama Title="eCollege">
            <controls:Panorama.Background>
                <ImageBrush ImageSource="/PanoramaBackground.png"/>
            </controls:Panorama.Background>
 
            <!--Panorama item one-->
            <controls:PanoramaItem Header="about me">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>ID:</TextBlock>
                        <TextBlock Text="{Binding Path=AuthenticatedUser.ID}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>Username:</TextBlock>
                        <TextBlock Text="{Binding Path=AuthenticatedUser.UserName}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>Firstname:</TextBlock>
                        <TextBlock Text="{Binding Path=AuthenticatedUser.FirstName}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>Lastname:</TextBlock>
                        <TextBlock Text="{Binding Path=AuthenticatedUser.LastName}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>Email:</TextBlock>
                        <TextBlock Text="{Binding Path=AuthenticatedUser.EmailAddress}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>Client String:</TextBlock>
                        <TextBlock Text="{Binding Path=AuthenticatedUser.ClientString}"/>
                    </StackPanel>
                </StackPanel>
            </controls:PanoramaItem>

            <!--Panorama item two-->
            <!--Use 'Orientation="Horizontal"' to enable a panel that lays out horizontally-->
            <controls:PanoramaItem Header="activity">
                <Grid>
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Coming soon!"/>
                </Grid>
            </controls:PanoramaItem>

            <!--Panorama item two-->
            <!--Use 'Orientation="Horizontal"' to enable a panel that lays out horizontally-->
            <controls:PanoramaItem x:Name="PanCourses" Header="courses">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Text="Number Of Courses"/>
                    <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Path=MyCourses.Count}"/>
                    <ListBox Grid.Row="2" ItemsSource="{Binding Path=MyCourses}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Azure" BorderThickness="2">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock>ID:</TextBlock>
                                            <TextBlock Text="{Binding Path=ID}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock>Title:</TextBlock>
                                            <TextBlock Text="{Binding Path=Title}"/>
                                        </StackPanel>
                                        <Button x:Name="BtnOpenCourse" Content="Open" Click="BtnOpenCourse_Click"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate> 
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </controls:PanoramaItem>

        </controls:Panorama>
    </Grid>
 
    <!--Panorama-based applications should not show an ApplicationBar-->

</phone:PhoneApplicationPage>